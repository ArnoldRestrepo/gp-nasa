export interface Apod {
  title: string;
  date: string;
  explanation: string;
  hdurl: string;
  url: string;
  mediaType: string;
  serviceVersion: string;
}

export interface EndpointApod { 
  title: string;
  date: string;
  explanation: string;
  hdurl: string;
  url: string;
  media_type: string;
  service_version: string;
}